services:
  - type: web
    name: 9router
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOSTNAME
        value: 0.0.0.0
      - key: DATA_DIR
        value: /app/data
      # Render injects PORT=10000 automatically — do not set it here
      - key: JWT_SECRET
        generateValue: true
      # INITIAL_PASSWORD not set — defaults to 123456 on first boot, change in dashboard after login.
      - key: SELF_PING_ENABLED
        value: true
      # External keep-warm (required to wake suspended container) — see .github/workflows/keepwarm.yml
      # Set repo Variable RENDER_URL=https://xxx.onrender.com to enable GitHub Actions pinger.
      # BASE_URL / NEXT_PUBLIC_BASE_URL not set by default — app defaults to localhost.
      # Set manually in Render Dashboard → Environment after first deploy if you need
      # cloud sync / OAuth callbacks on your https://xxx.onrender.com URL:
      #   BASE_URL=https://xxx.onrender.com
      #   NEXT_PUBLIC_BASE_URL=https://xxx.onrender.com
      #   CLOUD_URL=https://9router.com (optional)
    # --- Persistent disk (requires Starter plan+) ---
    # Free tier filesystem ephemeral — SQLite at /app/data lost on redeploy.
    # Uncomment for persistence (1GB plenty).
    # disk:
    #   name: 9router-data
    #   mountPath: /app/data
    #   sizeGB: 1

# Alternative: Native Node (no Docker). Uncomment to use instead of docker above.
#  - type: web
#    name: 9router-node
#    env: node
#    plan: free
#    buildCommand: npm install && npm run build
#    startCommand: npm start
#    healthCheckPath: /health
#    envVars:
#      - key: NODE_VERSION
#        value: 22.12.0
#      - key: NODE_ENV
#        value: production
#      - key: HOSTNAME
#        value: 0.0.0.0
#      - key: DATA_DIR
#        value: /opt/render/project/src/data
#      - key: JWT_SECRET
#        generateValue: true
#      # INITIAL_PASSWORD not set — defaults to 123456
